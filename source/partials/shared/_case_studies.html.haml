%ul.list-reset.row.gutters-none.no-xs-inset
  - data.work.each do |title, description|
    - slug = slugify(title)
    %li.col-sm-6
      = link_to "/work/#{slug}/", class: "banner banner-#{slug}" do
        %h2.banner-title= title
        %p.banner-subtitle= description
